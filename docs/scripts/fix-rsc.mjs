/**
 * Previously copied @vitejs/plugin-rsc from the monorepo root into the local
 * app's node_modules to avoid a Vite 5/Vite 8 conflict introduced by vitest.
 *
 * Since the docs app is now the monorepo root, there is only one node_modules
 * and this script is a no-op. Kept for compatibility with the postinstall hook.
 */
